# No Recovery Pattern Research

**Дата анализа:** 2026-04-16 / обновление по 5m и 15m: 2026-04-16  
**Период данных:** 2026-03-15 — 2026-04-15  
**Рынки:** BTC, ETH, SOL, XRP, DOGE, BNB — up/down, интервалы 5m / 15m / 60m

## Суть паттерна

Если сторона **no (Down)** падала до уровня ≤ X, а затем восстанавливалась до ≥ Y — win rate no значимо выше справедливого.

Стратегия: при касании no уровня ≤ 0.30 выставить лимитный ордер на покупку no по 0.70. Ордер заполняется автоматически если восстановление произойдёт.

## Базы данных

| База | Путь | Содержимое |
|------|------|-----------|
| Основная бэктест-база | `data/backtest.db` | таблицы `markets`, `pm_trades`, `binance_1s`, `binance_5s`, `coinbase_1s` |

Таблица `markets` — закрытые рынки с `winning_side`.  
Таблица `pm_trades` — все сделки (outcome=`Up`/`Down`, ts, price).

## Команды сбора данных

### 5-мин и 15-мин рынки (основная команда)

```bash
python3 -m research_bot.fetch_backtest_data --days 7
# опции:
#   --symbols BTC ETH SOL XRP   (default: все)
#   --force                     перескачать имеющиеся
#   --skip-binance              без Binance 1s klines
#   --skip-trades               без PM трейдов
```

### 60-мин (hourly) рынки

```bash
python3 -m research_bot.fetch_hourly --days 7
# опции:
#   --symbols BTC ETH SOL XRP
#   --force
```

Hourly рынки не попадают в стандартный /markets листинг — скрипт фетчит их через `/events?slug=` по сгенерированным slug-ам.

## Что в базе на момент анализа

| Интервал | Рынков | Период |
|----------|--------|--------|
| 5m  | 35 203 | 2026-03-15 — 2026-04-15 |
| 15m | 11 760 | 2026-03-15 — 2026-04-15 |
| 60m |  2 176 | 2026-03-15 — 2026-04-07 |

## Матрица win rate: дно → восстановление

Ставка на no при первом восстановлении до уровня ≥ Y, после касания ≤ X.  
Win rate одинаков для всех трёх интервалов (5m / 15m / 60m).

```
Дно \ Рост  >= 0.45  >= 0.50  >= 0.55  >= 0.60  >= 0.65  >= 0.70
<= 0.10      43-49%   48-55%   53-61%   55-65%   62-69%   66-74%
<= 0.15      44-49%   49-56%   54-62%   58-66%   64-71%   68-75%
<= 0.20      47-50%   52-56%   56-61%   61-66%   66-71%   69-75%
<= 0.25      48-49%   53-55%   57-61%   61-66%   66-71%   71-75%
<= 0.30      48-49%   54%      59-60%   63-65%   68-70%   73-74%
<= 0.35      48-50%   53-55%   59-60%   64%      69-70%   74%
```

Диапазон — разброс между 5m/15m/60m (очень малый, паттерн стабилен).

## Ключевой результат: дно ≤ 0.30 → ордер на 0.70

| | 5m | 15m | 60m |
|--|----|----|-----|
| Рынков где no касалась ≤ 0.30 | 17 012 | 5 799 | 1 474 |
| Ордер заполнился (восстановление до ≥ 0.70) | 6 083 (36%) | 2 198 (38%) | 580 (39%) |
| Win rate при заполнении | **74%** | **74%** | **73%** |
| Edge (wr − price) | +0.04 | +0.04 | +0.03 |

Maker fee на лимитных ордерах PM = 0%, поэтому edge не съедается комиссиями.

## Обновление: фокус на 5m и 15m

Ниже — актуальный срез только по `5m` и `15m` из `data/backtest.db`.

### Базовый паттерн 0.30 → 0.70, по символам

#### 5m

- всего fills: `6083`
- fill rate от touch `<=0.30`: `35.8%`
- `WR = 74.2%`
- `avg pnl/fill = +0.060` при stake `$1`

По символам:

| Symbol | touched | filled | fill rate | WR | pnl/fill |
|---|---:|---:|---:|---:|---:|
| BNB  | 74   | 28   | 37.8% | 89.3% | +0.276 |
| BTC  | 4210 | 1498 | 35.6% | 74.1% | +0.059 |
| DOGE | 86   | 29   | 33.7% | 69.0% | -0.015 |
| ETH  | 4306 | 1641 | 38.1% | 71.7% | +0.025 |
| SOL  | 4226 | 1491 | 35.3% | 75.7% | +0.082 |
| XRP  | 4110 | 1396 | 34.0% | 75.4% | +0.078 |

#### 15m

- всего fills: `2198`
- fill rate от touch `<=0.30`: `37.9%`
- `WR = 74.1%`
- `avg pnl/fill = +0.058` при stake `$1`

По символам:

| Symbol | touched | filled | fill rate | WR | pnl/fill |
|---|---:|---:|---:|---:|---:|
| BNB  | 14   | 8   | 57.1% | 75.0% | +0.071 |
| BTC  | 1454 | 551 | 37.9% | 73.5% | +0.050 |
| DOGE | 22   | 9   | 40.9% | 77.8% | +0.111 |
| ETH  | 1469 | 566 | 38.5% | 74.6% | +0.065 |
| SOL  | 1438 | 535 | 37.2% | 71.4% | +0.020 |
| XRP  | 1402 | 529 | 37.7% | 76.7% | +0.096 |

Промежуточный вывод:

- базовый паттерн воспроизводится и на `5m`, и на `15m`
- shape почти одинаковый
- `XRP` и `SOL` выглядят сильнее на `5m`
- `XRP` и `ETH` выглядят сильнее на `15m`
- `DOGE` и `BNB` выборка пока маленькая, но их уже нельзя игнорировать

### Обновление: 0.30 → 0.65 как более агрессивный вход

Для `5m`:

- fills: `6505`
- `WR = 69.4%`
- `avg pnl/fill = +0.068` при stake `$1`

Для `15m`:

- fills: `2332`
- `WR = 69.8%`
- `avg pnl/fill = +0.074` при stake `$1`

Tradeoff:

- `0.65` даёт больше fills и выше `avg pnl/fill`
- `0.70` даёт более высокий win rate
- для `5m/15m` оба уровня выглядят рабочими, но `0.65` интереснее как агрессивный вариант

### Как влияет нижняя точка при фиксированном entry = 0.65 (только 5m)

stake `$5`

| Bottom | fills | WR | total PnL | max DD | avg pnl/fill |
|---|---:|---:|---:|---:|---:|
| `<=0.20` | 3791  | 70.5% | +$1606.54 | $83.46  | +$0.424 |
| `<=0.25` | 5015  | 69.9% | +$1878.85 | $96.15  | +$0.375 |
| `<=0.30` | 6505  | 69.4% | +$2221.15 | $117.31 | +$0.341 |
| `<=0.35` | 8241  | 69.3% | +$2702.69 | $119.62 | +$0.328 |
| `<=0.40` | 10234 | 69.1% | +$3206.92 | $140.77 | +$0.313 |
| `<=0.45` | 12663 | 68.8% | +$3746.54 | $149.62 | +$0.296 |
| `<=0.50` | 16001 | 68.4% | +$4171.92 | $205.00 | +$0.261 |

Вывод:

- чем выше нижняя точка, тем больше абсолютный PnL
- но при этом:
  - падает `avg pnl/fill`
  - плавно снижается WR
  - растёт просадка
- `0.30` всё ещё выглядит хорошим компромиссом между scale и качеством

### Время touch и скорость recovery для 5m, сценарий 0.30 → 0.65

#### Когда произошёл first touch `<=0.30`

| Touch from start | n | WR | avg pnl/fill | total pnl |
|---|---:|---:|---:|---:|
| `<1m`   | 1919 | 68.9% | +0.303 | +581.92 |
| `1-2m`  | 1839 | 68.2% | +0.250 | +458.85 |
| `2-3m`  | 1262 | 71.1% | +0.468 | +590.00 |
| `3-4m`  | 791  | 72.3% | +0.563 | +445.00 |
| `4-5m`  | 694  | 67.7% | +0.209 | +145.38 |

Промежуточный вывод:

- лучшие touches происходят не сразу, а в середине рынка
- зона `2-4m` выглядит заметно сильнее ранних и совсем поздних touches

#### Сколько времени прошло от touch `0.30` до fill `0.65`

| Recovery delay | n | WR | avg pnl/fill | total pnl |
|---|---:|---:|---:|---:|
| `<15s`   | 516  | 64.9% | -0.006 | -3.08 |
| `15-30s` | 495  | 68.7% | +0.284 | +140.38 |
| `30-60s` | 1151 | 69.1% | +0.313 | +360.38 |
| `1-2m`   | 2006 | 68.8% | +0.292 | +585.38 |
| `2m+`    | 2337 | 71.3% | +0.487 | +1138.08 |

Промежуточный вывод:

- слишком быстрый recovery (`<15s`) почти не даёт edge
- чем дольше цена “отлежалась” после касания `0.30`, тем лучше
- самая сильная зона: `recovery >= 2m`

#### Комбинированный фильтр

Если для `5m`, `0.30 -> 0.65` оставить только:

- touch произошёл в окне `2-4m` от старта рынка
- recovery до `0.65` занял минимум `1m`

получается:

- `1263` ставок
- это `3.59%` всех `5m` рынков
- и `19.42%` от базовых fills `0.30 -> 0.65`
- `WR = 73.79%`
- `total pnl = +$854.23` при stake `$5`
- `avg pnl/fill = +$0.676`

Это означает:

- фильтр резко уменьшает число сделок
- но заметно улучшает качество входа
- этот паттерн стоит перепроверить на новой выборке, прежде чем делать из него production-правило

### Аналогичный разбор для 15m, сценарий 0.30 → 0.65

Ниже уже `stake = $5`, чтобы метрики были сопоставимы с 5m.

#### Как влияет нижняя точка при фиксированном entry = 0.65

| Bottom | fills | WR | total PnL | max DD | avg pnl/fill |
|---|---:|---:|---:|---:|---:|
| `<=0.20` | 1326 | 70.7% | +$585.38  | $78.46  | +$0.441 |
| `<=0.25` | 1784 | 70.6% | +$772.31  | $96.92  | +$0.433 |
| `<=0.30` | 2332 | 69.8% | +$863.08  | $114.23 | +$0.370 |
| `<=0.35` | 2939 | 69.6% | +$1028.08 | $121.15 | +$0.350 |
| `<=0.40` | 3622 | 69.0% | +$1105.38 | $130.77 | +$0.305 |
| `<=0.45` | 4574 | 68.0% | +$1045.38 | $143.85 | +$0.229 |
| `<=0.50` | 5565 | 67.6% | +$1113.46 | $165.77 | +$0.200 |

Вывод:

- картина очень похожа на `5m`
- более глубокий touch даёт лучшее `avg pnl/fill`, но меньше абсолютный scale
- зона `0.30-0.40` выглядит как основной компромисс
- если цель именно максимальный total PnL, на этой выборке чуть лучше `<=0.50`, но качество и просадка заметно хуже

#### Когда произошёл first touch `<=0.30`

Сначала грубые окна по `3m`:

| Touch from start | n | WR | avg pnl/fill | total pnl |
|---|---:|---:|---:|---:|
| `0-3m`   | 584 | 68.0% | +0.229 | +133.85 |
| `3-6m`   | 767 | 68.5% | +0.265 | +203.46 |
| `6-9m`   | 426 | 73.2% | +0.634 | +270.00 |
| `9-12m`  | 266 | 68.8% | +0.292 | +77.69 |
| `12-15m` | 139 | 75.5% | +0.811 | +112.69 |

Детальнее по минутным окнам:

- `0-1m`: `153` ставок, `WR 65.4%`, `avg +0.028`
- `1-2m`: `150` ставок, `WR 71.3%`, `avg +0.487`
- `2-3m`: `281` ставок, `WR 67.6%`, `avg +0.201`
- `3-4m`: `318` ставок, `WR 66.4%`, `avg +0.104`
- `4-5m`: `265` ставок, `WR 67.6%`, `avg +0.196`
- `5-6m`: `184` ставок, `WR 73.4%`, `avg +0.644`
- `6-7m`: `181` ставок, `WR 69.1%`, `avg +0.312`
- `7-8m`: `127` ставок, `WR 71.7%`, `avg +0.512`
- `8-9m`: `118` ставок, `WR 81.4%`, `avg +1.258`
- `9-10m`: `97` ставок, `WR 69.1%`, `avg +0.313`
- `10-11m`: `92` ставок, `WR 70.7%`, `avg +0.435`
- `11-12m`: `77` ставок, `WR 66.2%`, `avg +0.095`
- `12-13m`: `60` ставок, `WR 76.7%`, `avg +0.897`
- `13-14m`: `44` ставок, `WR 72.7%`, `avg +0.594`
- `14-15m`: `35` ставок, `WR 77.1%`, `avg +0.934`

Промежуточный вывод:

- ранние touches (`0-6m`) слабее
- сильная зона начинается примерно с `6m`
- особенно интересно выглядят `6-9m`, а внутри них пик в `8-9m`
- очень поздние touches (`12-15m`) сильные, но выборка пока маленькая

#### Сколько времени прошло от touch `0.30` до fill `0.65`

| Recovery delay | n | WR | avg pnl/fill | total pnl |
|---|---:|---:|---:|---:|
| `<30s`   | 114 | 64.0% | -0.074 | -8.46 |
| `30-60s` | 102 | 69.6% | +0.354 | +36.15 |
| `1-2m`   | 286 | 70.3% | +0.406 | +116.15 |
| `2-4m`   | 648 | 72.1% | +0.544 | +352.31 |
| `4-8m`   | 797 | 68.1% | +0.241 | +191.92 |
| `8m+`    | 385 | 70.9% | +0.455 | +175.00 |

Промежуточный вывод:

- как и на `5m`, слишком быстрый recovery почти бесполезен
- лучшая зона по quality сейчас выглядит как `2-4m`
- `4-8m` уже слабее, то есть на `15m` зависимость не монотонная

#### Комбинированные окна

Для `15m`, `0.30 -> 0.65`, несколько кандидатов:

| Filter | fills | % all 15m | % base fills | WR | total PnL | avg pnl/fill | max DD |
|---|---:|---:|---:|---:|---:|---:|---:|
| base | 2332 | 19.83% | 100.00% | 69.81% | +$863.08 | +$0.370 | $114.23 |
| `touch 3-9m` + `recovery >= 1m` | 1139 | 9.69% | 48.84% | 70.24% | +$458.85 | +$0.403 | $51.92 |
| `touch 3-9m` + `recovery >= 2m` | 1007 | 8.56% | 43.18% | 70.41% | +$418.85 | +$0.416 | $49.23 |
| `touch 6-12m` + `recovery >= 1m` | 630 | 5.36% | 27.02% | 72.54% | +$365.38 | +$0.580 | $42.31 |
| `touch 6-12m` + `recovery >= 2m` | 534 | 4.54% | 22.90% | 72.28% | +$299.23 | +$0.560 | $48.46 |
| `touch 6-12m` + `recovery >= 4m` | 292 | 2.48% | 12.52% | 71.23% | +$140.00 | +$0.479 | $34.23 |
| `touch 9-12m` + `recovery >= 2m` | 190 | 1.62% | 8.15% | 70.53% | +$80.77 | +$0.425 | $44.23 |
| `touch 9-15m` + `recovery >= 2m` | 228 | 1.94% | 9.78% | 71.93% | +$121.54 | +$0.533 | $43.85 |

Вывод:

- для качества входа лучший кандидат сейчас: `touch 6-12m` + `recovery >= 1-2m`
- он резко режет число входов, но заметно поднимает `WR` и `avg pnl/fill`
- если цель total PnL, base всё ещё сильнее

#### Live-совместимые прогоны

Важно: ниже уже не оффлайн-фильтры по будущему, а честная симуляция:

1. дождались `touch <= 0.30`
2. подождали `X` секунд
3. если в этот момент цена всё ещё `< 0.65`, ставим лимитку `0.65`
4. дальше считаем fill только после этого момента

| Live wait | fills | % base fills | WR | total PnL | avg pnl/fill | max DD |
|---|---:|---:|---:|---:|---:|---:|
| base | 2332 | 100.00% | 69.81% | +$863.08 | +$0.370 | $114.23 |
| `15s`  | 2278 | 97.68% | 70.11% | +$894.62 | +$0.393 | $109.23 |
| `30s`  | 2242 | 96.14% | 70.21% | +$897.69 | +$0.400 | $111.92 |
| `60s`  | 2154 | 92.37% | 70.24% | +$868.46 | +$0.403 | $112.69 |
| `120s` | 1939 | 83.15% | 70.04% | +$751.15 | +$0.387 | $93.46 |

Вывод:

- для `15m` live-ожидание работает лучше, чем для `5m`
- лучшие варианты из проверенных: `30s` и `15s`
- `30s` сейчас лучший по total PnL: `+$897.69` против `+$863.08` у base
- `15s` и `30s` также немного уменьшают drawdown

## PnL симуляция: $1 на ордер, все рынки

Период: 24 дня, апрель 2026.

| | 5m | 15m | 60m | Итого |
|--|----|----|-----|-------|
| Ставок/день | 253 | 92 | 24 | ~369 |
| Вложено/день | ~$177 | ~$64 | ~$17 | ~$258 |
| PnL/день | **+$15.23** | **+$5.32** | **+$0.95** | **+$21.50** |
| ROI | ~8.6% | ~8.3% | ~5.6% | ~8.3% |
| Прибыльных дней | 23/24 (96%) | 17/24 (71%) | 16/24 (67%) | |

## Как воспроизвести анализ

Все расчёты — inline Python, данные из `data/backtest.db`.

### Матрица win rate

```python
import sqlite3
from collections import defaultdict

DB = 'data/backtest.db'
conn = sqlite3.connect(DB)
conn.row_factory = sqlite3.Row

BOTTOMS    = [0.10, 0.15, 0.20, 0.25, 0.30, 0.35]
RECOVERIES = [0.45, 0.50, 0.55, 0.60, 0.65, 0.70]
INTERVAL   = 5  # или 15, 60

markets = conn.execute(
    'SELECT market_id, winning_side FROM markets WHERE interval_minutes=? AND winning_side IS NOT NULL',
    (INTERVAL,)
).fetchall()

grid = {(b, r): [0, 0] for b in BOTTOMS for r in RECOVERIES}

for mkt in markets:
    mid = mkt['market_id']
    winning_side = mkt['winning_side']

    trades = conn.execute(
        "SELECT ts, price FROM pm_trades WHERE market_id=? AND outcome='Down' ORDER BY ts",
        (mid,)
    ).fetchall()
    if not trades:
        continue

    prices = [(t['ts'], t['price']) for t in trades]

    for bottom in BOTTOMS:
        touch_ts = next((ts for ts, p in prices if p <= bottom), None)
        if touch_ts is None:
            continue
        after = [p for ts, p in prices if ts > touch_ts]
        for recovery in RECOVERIES:
            if recovery > bottom and any(p >= recovery for p in after):
                if winning_side == 'no':
                    grid[(bottom, recovery)][0] += 1
                else:
                    grid[(bottom, recovery)][1] += 1

for b in BOTTOMS:
    for r in RECOVERIES:
        if r <= b:
            continue
        w, l = grid[(b, r)]
        n = w + l
        if n:
            print(f'Дно<={b:.2f} Рост>={r:.2f}: {w/n:.0%} n={n}')
```

### PnL по дням

```python
import sqlite3
from collections import defaultdict

DB = 'data/backtest.db'
STAKE = 1.0; BOTTOM = 0.30; ENTRY = 0.70
INTERVAL = 5  # или 15, 60

conn = sqlite3.connect(DB)
conn.row_factory = sqlite3.Row

markets = conn.execute(
    'SELECT market_id, winning_side, market_end FROM markets WHERE interval_minutes=? AND winning_side IS NOT NULL',
    (INTERVAL,)
).fetchall()

by_day = defaultdict(list)

for mkt in markets:
    mid = mkt['market_id']
    winning_side = mkt['winning_side']
    day = mkt['market_end'][:10]

    trades = conn.execute(
        "SELECT ts, price FROM pm_trades WHERE market_id=? AND outcome='Down' ORDER BY ts",
        (mid,)
    ).fetchall()
    if not trades:
        continue

    touch_ts = next((t['ts'] for t in trades if t['price'] <= BOTTOM), None)
    if touch_ts is None:
        continue
    if not any(t['price'] >= ENTRY for t in trades if t['ts'] > touch_ts):
        continue

    pnl = STAKE / ENTRY - STAKE if winning_side == 'no' else -STAKE
    by_day[day].append(pnl)

total = sum(p for ps in by_day.values() for p in ps)
n_days = len(by_day)
print(f'PnL/день: ${total/n_days:+.2f}  |  всего ${total:+.2f} за {n_days} дней')
```

## Выводы

1. Паттерн стабильно воспроизводится на `5m` и `15m`; hourly пока вторичен.
2. Для `5m/15m` уровни `0.65` и `0.70` оба рабочие:
   - `0.65` агрессивнее и даёт больше fills
   - `0.70` консервативнее и даёт выше WR
3. Для `5m`, `entry = 0.65`, нижняя точка `0.30` выглядит хорошим компромиссом между scale, качеством и drawdown.
4. Для `5m`, `0.30 -> 0.65`, полезный дополнительный фильтр может быть завязан на время:
   - touch в `2-4m` от старта
   - recovery не быстрее `1m`
5. Maker fee = 0% — edge на пассивной лимитке не съедается комиссией.
6. Следующий шаг — перепроверить временные паттерны на новой выгрузке, прежде чем внедрять фильтр в боевой код.
